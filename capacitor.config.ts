import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nutritionwise.app',
  appName: 'NutritionWise',
  webDir: 'dist/public'
};

export default config;
